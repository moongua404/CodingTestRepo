#include <iostream>
using namespace std;
int main() {
	int N;
	cin >> N;
	char* S = new char[N];
	cin >> S;
	for (int i = 0; i < N; i++) {
		cout << (int(S[i]) >= 97 ? char(S[i] - 32) : char(S[i] + 32));
	}

	return 0;
}
